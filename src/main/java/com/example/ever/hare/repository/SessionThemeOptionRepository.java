package com.example.ever.hare.repository;

import com.example.ever.hare.entity.SessionThemeOption;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SessionThemeOptionRepository extends JpaRepository<SessionThemeOption, Long> {
}
